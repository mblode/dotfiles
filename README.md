# mblode's dotfiles
### Install
```
chsh -s $(which zsh)
git clone https://github.com/mblode/dotfiles.git ~/.dotfiles
cd ~/.dotfiles
make
```

### Update
```
cd ~/.dotfiles
make update
```

### Install Homebrew
`./brew-install.sh`

### Install Atom Packages
`./atome-install.sh`
