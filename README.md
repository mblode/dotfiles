# mblode's dotfiles
### Install
```
chsh -s $(which zsh)
git clone https://github.com/mblode/dotfiles.git
cd ~/dotfiles
make
```

### Install Homebrew
`./brew-install.sh`

### Install Sublime Packages
`./sublime-install.sh`
